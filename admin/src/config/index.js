// 应用配置
const config = {
  // API基础URL（用于API请求）
   apiBaseURL: 'http://localhost:8080/api',
  // 文件服务基础URL（用于访问静态文件，不包含/api）
   fileBaseURL: 'http://localhost:8080:8080/api'
}

export default config

