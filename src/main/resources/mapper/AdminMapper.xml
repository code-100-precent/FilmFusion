<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cxdproject.coder.mapper.AdminMapper">

    <resultMap id="BaseResultMap" type="cn.cxdproject.coder.model.entity.Admin">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="avatar" property="avatar" />
        <result column="role" property="role" />
        <result column="enabled" property="enabled" />
        <result column="deleted" property="deleted" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
            `id`,
            `username`,
            `password`,
            `email`,
            `avatar`,
            `role`,
            `enabled`,
            `deleted`,
            `created_at`,
            `updated_at`
    </sql>

    <select id="existsByUsername" resultType="java.lang.Boolean">
        SELECT COUNT(1) FROM admin
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="existsByEmail" resultType="java.lang.Boolean">
        SELECT COUNT(1) FROM admin
        WHERE email = #{email} AND deleted = 0
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM admin
        WHERE email = #{email} AND deleted = 0
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM admin
        WHERE username = #{username} AND deleted = 0
    </select>
</mapper>
